<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Se a requisição não for para a pasta public/, redireciona internamente para public/
    RewriteCond %{REQUEST_URI} !/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
